# Agents

## Cursor Cloud specific instructions

This repository ("Appnicofit") is currently an empty project scaffold with only a `README.md`. There are no source files, dependencies, build systems, linters, test frameworks, or services to run.

**Current state (as of initial setup):**
- Single file: `README.md`
- No package manager, language runtime, or framework chosen yet
- No lint, test, or build commands available
- No services or databases required

Once the project gains source code and dependencies, this section should be updated with:
- How to install dependencies (e.g., `npm install`, `pip install -r requirements.txt`)
- How to run lint, tests, and build
- How to start development servers and any required backing services
- Any non-obvious caveats discovered during setup
